diff -rua jquerytools/src/dateinput/dateinput.js at/src/dateinput/dateinput.js
--- jquerytools/src/dateinput/dateinput.js	2014-05-06 14:47:55.000000000 +0800
+++ at/src/dateinput/dateinput.js	2014-05-06 15:16:06.000000000 +0800
@@ -300,7 +300,7 @@
 			e || (e = $.Event("api"));
 
 			// focus the input after selection (doesn't work in IE)
-			if (e.type == "click" && !$.browser.msie) {
+			if (e && e.type == "click" && !$.browser.msie) {
 				input.focus();
 			}
 			
diff -rua jquerytools/src/tooltip/tooltip.js at/src/tooltip/tooltip.js
--- jquerytools/src/tooltip/tooltip.js	2014-05-06 14:47:55.000000000 +0800
+++ at/src/tooltip/tooltip.js	2014-05-06 15:16:06.000000000 +0800
@@ -197,9 +197,17 @@
 						tip = $(conf.tip).eq(0);
 						
 					// autogenerated tooltip
-					} else if (title) { 
-						tip = $(conf.layout).addClass(conf.tipClass).appendTo(document.body)
-							.hide().append(title);
+					} else if (title) {
+						var ee = $(conf.layout),
+						el = ee;
+
+						tip = ee.addClass(conf.tipClass).appendTo(document.body).hide();
+						
+						while (ee.length) {
+							el = ee;
+							ee = ee.children();
+						}
+						el.append(title);
 
 					// manual tooltip
 					} else {	
