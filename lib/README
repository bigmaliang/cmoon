用途
===
lib 目录存放 moon 依赖的公共库文件(不包含第三方库，如mysql、conv等), 包含但不限于C, JS文件.



目录结构
======

lib/
|-- mevent        高速数据后端客户端 API (mevent.skel.h)
|-- mnl            moon library, moon 自己编写的C库文件 (mheads.h)
`-- js            公用js文件


约定
===

mnl 目录当前仅实现一些 moon 需要用到的基础函数, 例如cgi的日志, 数据库封装等,
由于 moon 本身可能同时有多个站点在开发, 所以这里的函数都是'站点无关'的.
对于特定站点的公用代码有其自身的库目录, 请不要放在mnl里.

js 目录功能和编程注意事项同 mnl.
