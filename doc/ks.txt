1: Linux文件权限及修改方法; 软链接与硬链接的创建方法、及区别

2: 写一个去掉字符串开始和结尾特定字符的函数(如:输入 "aa newa word aaa", 'a'; 输出 " newa word ")：

3: 现有rand(x) 函数返回 0~x 之间的随机整数，限调用一次rand()，请写出表达式返回 -3 ~ 3 之间的任意整数。

4: 现某表内有N条数据，请设计一种较优方法，同时满足以下功能：
    1.支持对这N条数据的排序
    2.支持新增和删除指定位置
    3.支持调整某行至指定位置
    比如：最简单的方法，为每行新增个order字段，指明该行的排序顺序。

5: 请描述socket通信常用的两种协议及其区别，以及同步、异步socket通信的区别及应用场景

6: 请列举两个你认为有用的c99语法

7: 请简单描述程序预处理、编译、链接、执行的过程

8: 简单描述及评价一下你使用过的互联网开发常用软件

9: 简单描述及评价你觉得最有价值的项目及软件(可以是自己参与过的，也可以是仅仅使用过的)

10: 请简单解释(可以图解)以下两个函数参数的内存操作
    int mdb_init(mdb_conn **conn)
    {
        int ret;
        mdb_conn *lconn = NULL;
    
        if (!conn) return 1;
        
        *conn = NULL;
    
        ret = connect("mysql", &lconn);
        if (ret) return 1;
    
        lconn->dsn = strdup(drv_dsn);
    
        *conn = lconn;
    
        return 0;
    }
    
    char* mstr_rep(char *s, char f, char t)
    {
      int x;
    
      x = strlen(s) - 1;

      while (x>=0 && s[x]==f) s[x--] = t;
    
      while (*s && *s==f) *s++ = t;
      
      return s;
    }


