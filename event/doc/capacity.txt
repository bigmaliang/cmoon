事件中心设计容量
==============

由于内存限制，当前事件中心对每种事件单机（2G内存机器）最大支持缓存100万个操作。
后端处理过慢导致缓存队列满的时候会返回REP_ERR_BUSY的错误号给客户端。

入事件中心的队列操作最快支持每秒15000~20000个，所以事件中心理论上讲只能支持满负荷入队
1分钟左右，满负荷1分钟后，后续的请求都将被抛弃。

因为后台无法记录没有入队列的操作，出于性能考虑，也不会将这些数据写磁盘。
所以在有大量并发请求情况时，客户端应该检查返回值，判断入事件中心队列的操作是否正常。

