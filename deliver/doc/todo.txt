ml
	- * 关掉浏览器再上, 又有个 JOIN 消息, 会列出两同样图像, 此时需要之前 LEFT, 再 JOIN
	- * 尽量加上浏览人数功能, 不行就去掉
	- * 访问历史列表去重
	统计数据入库
	访客群功能用户管理后台

	压力测试

dl
	- * 聊天功能实现: 群聊, 单P
	* 屏蔽功能(开始聊天时的是否[被]屏蔽, 包括群 和 聊天过程中的屏蔽动作. 群中屏蔽时需将此人踢出)
	- * 聊天人数的即时更新

xl
	* 设置页面: 关闭聊天, 黑名单管理
	* 表情功能: 切图, text 解释
	= * 消息提示: 闪烁, 气泡
	细节:
		群聊新消息无提示
		新消息标题滚动提示, 声音提示
		单个聊天列表滚动切换体验不好
		聊天窗口除右上角图标触发开启, 关闭, 最好能整个title 都触发
		缩小时的气泡功能
		屏蔽按钮后窗口中提示消息, 对方发送消息后的提示
		鼠标移到 "发送" 上提示 Control+Enter 发送
		
		- 群聊不能屏蔽
		- 群聊无历史消息
		- 收到消息没滚屏
		- 聊天窗口时, 整个收起, 只收了列表
		- 点击聊天时默认弹开群聊窗口
		- 空白时, 收到人家发送的消息, 会重复
		
		列表中标识自己
		列表中男女标志
		随机速聊功能
		回车变 <br />
		清屏功能
		关闭浏览器时, 发送退出命令
		中文昵称变乱码
		
		字体, 颜色设置
		群公告设置

	
屏蔽, 表情, 设置
